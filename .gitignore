# Personal health data / regenerable outputs (contain therapy data + device serial)
dump.txt
events.csv
sessions.csv
*.dump
sleephq/out/

# Python
__pycache__/
*.pyc
venv/

# Settings backups/snapshots (contain device serial)
settings-backup-*.json
blob-baseline.json

# Vendor manuals (Somnetics copyright — get from mytranscend.com; see docs/NOTES.md)
docs/*.pdf
